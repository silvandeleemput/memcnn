[tox]
envlist = {py27,py36}-torch{0.4.0,1.0.0,1.1.0}
skipsdist=True

[testenv:test]
passenv = LC_ALL, LANG, HOME
commands = pytest
deps =
   -rdevRequirements.txt

[testenv:release]
deps=
  bumpversion
commands=
 bumpversion --dry-run minor

# generate the sphinx doc
[testenv:docs]
basepython=python
changedir=docs
deps= -rdocsRequirements.txt
commands=
    sphinx-build -b linkcheck -b html -d {envtmpdir}/doctrees . {envtmpdir}/html
